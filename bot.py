import asyncio
import logging
import random
import os
import requests
import tempfile
from datetime import datetime, timedelta
from telegram import Bot, InputMediaPhoto
from telegram.error import TelegramError
from telegram.request import HTTPXRequest
import schedule
import time
from threading import Thread
from bs4 import BeautifulSoup
from PIL import Image
import io
import httpx
import signal
import sys
from elevenlabs.client import ElevenLabs
from elevenlabs import play
import re

# Настройка логирования
logging.basicConfig(
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    level=logging.INFO
)
logger = logging.getLogger(__name__)

# Конфигурация бота
BOT_TOKEN = os.getenv("BOT_TOKEN", "8437007902:AAFXbYzoWZI7lmg4EvF3DcopKXwbzYQgpkI")
CHANNEL_ID = os.getenv("CHANNEL_ID", "-1002722697999")
ELEVENLABS_API_KEY = os.getenv("ELEVENLABS_API_KEY")
ELEVENLABS_VOICE_ID = os.getenv("ELEVENLABS_VOICE_ID")

class WorkBot:
    def __init__(self):
        # Настройка HTTP клиента
        request = HTTPXRequest(
            connection_pool_size=10,
            pool_timeout=30,
            read_timeout=30,
            write_timeout=30,
            connect_timeout=30,
            http_version="1.1"
        )
        self.bot = Bot(token=BOT_TOKEN, request=request)
        self.last_keywords = []
        self.last_message_id = None
        
        # Система предотвращения коллизий
        self.recent_messages = []
        self.recent_works = []
        self.max_recent_messages = 20
        self.max_recent_works = 50
        
        # Инициализация ElevenLabs клиента
        if ELEVENLABS_API_KEY:
            self.elevenlabs_client = ElevenLabs(api_key=ELEVENLABS_API_KEY)
            logger.info("✅ ElevenLabs клиент инициализирован")
        else:
            self.elevenlabs_client = None
            logger.warning("⚠️ ElevenLabs API ключ не найден, голосовые сообщения будут использовать gTTS")
        
        # Базовые элементы для генерации работ
        self.work_verbs = [
            "Покрасить", "Помыть", "Убрать", "Покосить", "Посидеть", 
            "Разобрать", "Покормить", "Почистить", "Поклеить", "Повесить",
            "Собрать", "Разложить", "Переставить", "Отремонтировать", "Починить",
            "Настроить", "Проверить", "Загрузить", "Выгрузить", "Перевезти",
            "Доставить", "Забрать", "Принести", "Отнести", "Установить", "Снять"
        ]
        
        self.work_objects = [
            "забор", "дом", "машину", "окна", "полы", "стены", "двери", 
            "крышу", "подвал", "чердак", "балкон", "лестницу", "ворота",
            "калитку", "лавочку", "скамейку", "почтовый ящик", "люстру",
            "зеркала", "мебель", "технику", "инструменты", "одежду", "обувь",
            "посуду", "ковры", "шторы", "радиаторы", "трубы", "краны",
            "розетки", "выключатели", "лампочки", "провода", "кондиционер",
            "печь", "камин", "холодильник", "стиральную машину", "пылесос",
            "телефон", "компьютер", "телевизор", "велосипед", "сарай",
            "гараж", "беседку", "крыльцо", "дорожки", "клумбы", "газон",
            "бассейн", "колодец", "септик", "антенну", "вентиляцию"
        ]
        
        self.work_conditions = [
            "от грязи", "от пыли", "от мусора", "от листьев", "от снега",
            "от ржавчины", "от плесени", "от паутины", "от пятен",
            "до блеска", "аккуратно", "быстро", "качественно", "срочно",
            "в субботу", "в воскресенье", "завтра", "сегодня", "на выходных"
        ]
        
        # Цены
        self.prices = [
            "1000₽", "1200₽", "1500₽", "1800₽", "2000₽", "2300₽", "2500₽",
            "2800₽", "3000₽", "3500₽", "4000₽", "4500₽", "5000₽"
        ]
        
        # Смешные бонусы и подарки (УЛУЧШЕННЫЕ)
        self.bonuses = [
            # Нормальные бонусы
            "такси с меня",
            "обед даю",
            "обед борщ",
            "обед + чай",
            "материалы есть",
            "инвентарь даю",
            "инструменты предоставлю",
            "чай с печеньками",
            
            # Смешные подарки
            "в подарок бу клей",
            "в подарок старую отвертку",
            "в подарок ржавый болт",
            "в подарок сломанный молоток",
            "в подарок дырявое ведро",
            "в подарок кривые плоскогубцы",
            "в подарок тупую пилу",
            "в подарок погнутый гвоздь",
            "в подарок полупустую банку краски",
            "в подарок старый веник",
            
            # Очень смешные абсурдные подарки
            "в подарок носок (один)",
            "в подарок пустую бутылку из-под кваса",
            "в подарок старую газету за 2003 год",
            "в подарок фантик от конфеты",
            "в подарок сломанную зажигалку",
            "в подарок кассету с шансоном",
            "в подарок фото Путина",
            "в подарок календарик с девушкой",
            "в подарок старый журнал за туалетом почитать",
            "в подарок упаковку просроченных семечек",
            "в подарок половину сигареты",
            "в подарок пакет майонеза просрочка",
            "в подарок батон 3-дневной давности",
            "в подарок пустую пачку сигарет",
            "в подарок жвачку б/у",
            "в подарок камень с улицы (красивый)",
            "в подарок палку (прочную)",
            "в подарок коробок спичек (половина использована)",
            "в подарок старую расческу",
            "в подарок чужой паспорт (найден)",
            "в подарок флешку 256мб (может работает)",
            "в подарок дискету 3.5",
            "в подарок кассетный плеер без батареек",
            "в подарок телефон Nokia 3310 (экран треснут)",
            "в подарок зарядку от неизвестного телефона",
            "в подарок чужое письмо не вскрытое",
            "в подарок старую визитку",
            "в подарок рецепт от врача (использованный)",
            "в подарок просроченный талон на массаж",
            "в подарок чек из магазина (на удачу)",
            "в подарок фотку бывшей (некрасивая)",
        ]
        
        # Дополнительная информация (УЛУЧШЕННАЯ)
        self.extra_info = [
            # Нормальные условия
            "за пару часов",
            "на руки",
            "быстро сделать",
            "срочно нужно",
            "деньги сразу",
            "наличными",
            "опыт не важен",
            "главное желание",
            
            # Смешные условия
            "собака злая но на цепи",
            "бабушка ворчит но не кусается",
            "сосед шумный но похуй",
            "там вайфай работает",
            "можно покурить",
            "туалет есть (на улице)",
            "кофе не обещаю но чифир сделаю",
            "квас в погребе (пойдет)",
            "самогон могу дать попробовать",
            "если хорошо сделаешь еще раз позову",
            "соседи не ебут мозг",
            "работать можно в трусах если жарко",
            "музыку слушать можно (русский шансон)",
            "перекур каждые 15 минут",
            "пиво после работы",
            "можно на скутере приехать",
            "главное не обоссы дом",
            "аккуратно а то жена убьет",
            "тихо а то соседи вызовут",
            "быстро а то дождь пойдет",
            "качественно а то не заплачу",
        ]
        
        # Угарные дополнения (P.S.)
        self.quirky_additions = [
            "забор красить аккуратно",
            "бабушка ворчит но добрая",
            "крыльцо до блеска отмыть",
            "снег убирать осторожно",
            "траву косить ровно",
            "мусор убирать весь",
            "собака лает но не кусается",
            "соседи ворчат но терпят",
            "деньги дам сразу после работы",
            "материалы все есть",
            
            # НОВЫЕ УГАРНЫЕ
            "жена дома будет следить",
            "кот может покусать будь осторожен",
            "электричество периодически вырубается",
            "в доме холодно одевайся теплее",
            "соседи алкаши не обращай внимание",
            "машина старая аккуратнее с ней",
            "забор уже падает просто подпили",
            "краска вонючая открой окна",
            "лестница шатается держись крепче",
            "крыша протекает работай быстрее",
            "в подвале крысы не спускайся",
            "бабушка глухая орать не надо",
            "тесть приедет надо до вечера",
            "свадьба завтра успеть надо",
            "жена беременная нервы не трепли",
            "теща приехала быстрее делай",
        ]
        
        # Фразы для ответных сообщений (работа выполнена)
        self.completion_phrases = [
            "уже сделали, спасибо",
            "работа выполнена, все четко",
            "сделали быстро и качественно",
            "готово, спасибо за работу",
            "выполнили, все как надо",
            "сделали, работали хорошо",
            "готово, качественно поработали",
            "выполнили быстро",
            "сделали на совесть",
            "работали как звери",
            "все четко сделали",
            "быстро и хорошо",
            "работали отлично",
            "сделали на ура",
            "красавцы, спасибо",
            "молодцы ребята",
            "так держать",
            "вот это работа",
            "профессионалы блин",
            "еще позову если что",
        ]
        
        # Смешные подарки для завершенных работ
        self.completion_details = [
            "еще и 15 гвоздей подарили",
            "еще и флешку 512мб дали",
            "еще и старую батарейку оставили",
            "еще и ржавый болт подарили",
            "еще и сломанную отвертку дали",
            "еще и пустую банку краски оставили",
            "еще и кусок проволоки подарили",
            "еще и старую тряпку дали",
            "еще и пустую бутылку оставили",
            "еще и сломанный карандаш подарили",
            "еще и ржавую шайбу дали",
            "еще и кусок скотча оставили",
            "еще и старую газету подарили",
            "еще и сломанную лампочку дали",
            "еще и пустую коробку оставили",
            
            # НОВЫЕ СМЕШНЫЕ
            "еще и пачку сигарет дали (3 штуки внутри)",
            "еще и литр самогона налили",
            "еще и шаурму купили",
            "еще и пиво взяли",
            "еще и на такси денег дали",
            "еще и чаевых накинули 100 рублей",
            "еще и пирожок дали",
            "еще и чаю напоили",
            "еще и с собой борща налили",
            "еще и курицу жареную дали",
            "еще и котлету дали",
            "еще и блинов напекли",
            "еще и компот дали",
            "еще и огурцов соленых дали",
            "еще и сала кусок дали",
        ]
    
    def generate_work_type(self):
        """Генерирует тип работы с предотвращением повторений"""
        max_attempts = 20
        
        for attempt in range(max_attempts):
            verb = random.choice(self.work_verbs)
            obj = random.choice(self.work_objects)
            
            # 70% шанс добавить условие
            if random.random() < 0.7:
                condition = random.choice(self.work_conditions)
                work = f"{verb} {obj} {condition}"
            else:
                work = f"{verb} {obj}"
            
            # Проверяем уникальность
            if work not in self.recent_works:
                self.recent_works.append(work)
                if len(self.recent_works) > self.max_recent_works:
                    self.recent_works.pop(0)
                return work
        
        # Если не получилось, возвращаем хотя бы что-то
        return work
    
    def generate_message(self):
        """Генерирует сообщение в стиле объявления о работе"""
        max_attempts = 30
        
        for attempt in range(max_attempts):
            work = self.generate_work_type()
            price = random.choice(self.prices)
            bonus = random.choice(self.bonuses)
            
            # Формируем сообщение
            message = f"{work}, {price}, {bonus}"
            
            # 70% шанс добавить дополнительную информацию
            if random.random() < 0.7:
                extra = random.choice(self.extra_info)
                message += f", {extra}"
            
            # 50% шанс добавить еще одну дополнительную информацию
            if random.random() < 0.5:
                extra2 = random.choice(self.extra_info)
                if extra2 not in message:
                    message += f", {extra2}"
            
            # 40% шанс добавить P.S.
            if random.random() < 0.4:
                quirky = random.choice(self.quirky_additions)
                message += f", {quirky}"
            
            # Добавляем легкую безграмотность
            message = self.add_typos(message)
            
            # Делаем с маленькой буквы
            message = message[0].lower() + message[1:] if message else message
            
            # Проверяем уникальность
            if message not in self.recent_messages:
                self.recent_messages.append(message)
                if len(self.recent_messages) > self.max_recent_messages:
                    self.recent_messages.pop(0)
                return message
        
        return message
    
    def add_typos(self, text):
        """Добавляет легкую безграмотность"""
        replacements = {
            "Покрасить": "Пакрасить",
            "Помыть": "Памыть",
            "Покосить": "Пакосить",
            "Посидеть": "Пасидеть",
            "Покормить": "Пакормить",
            "крыши": "крышы",
            "велосипед": "веласипед",
            "хорошо": "харашо",
            "качественно": "качественна",
            "аккуратно": "акуратна",
            "материалы": "матириалы",
        }
        
        for old, new in replacements.items():
            text = text.replace(old, new)
        
        return text
    
    def generate_completion_message(self):
        """Генерирует сообщение о завершении работы"""
        phrase = random.choice(self.completion_phrases)
        
        # 60% шанс добавить подарок
        if random.random() < 0.6:
            detail = random.choice(self.completion_details)
            message = f"{phrase}, {detail}"
        else:
            message = phrase
        
        return message.lower()
    
    def convert_to_personal_voice_style(self, text):
        """Конвертирует текст для голосового сообщения"""
        result = text.lower()
        result = result.replace("₽", " рублей")
        
        # Сельские выражения
        rural_expressions = [
            "ну типа", "как бы", "вот так вот", "знаешь ли", "понимаешь",
            "слушай", "короче", "в общем", "кстати"
        ]
        
        # 50% шанс добавить выражение в начало
        if random.random() < 0.5:
            expression = random.choice(rural_expressions)
            result = f"{expression}, {result}"
        
        # Добавляем обращения
        if random.random() < 0.5:
            start_words = ["братан, ", "земляк, ", "друг, ", "парень, "]
            result = random.choice(start_words) + result
        
        if random.random() < 0.4:
            end_words = [", братан", ", земляк", ", друг", ", понял"]
            result = result + random.choice(end_words)
        
        return result.strip()
    
    def improve_russian_pronunciation(self, text):
        """Улучшает произношение для TTS"""
        result = text.replace("  ", " ").replace("₽", " рублей")
        return result.strip()
    
    def generate_voice_message(self, text):
        """Генерирует голосовое сообщение"""
        try:
            # Конвертируем текст для голоса
            personal_text = self.convert_to_personal_voice_style(text)
            personal_text = self.improve_russian_pronunciation(personal_text)
            
            logger.info(f"Текст для озвучки: {personal_text}")
            
            # Используем ElevenLabs если доступен
            if self.elevenlabs_client and ELEVENLABS_VOICE_ID:
                logger.info("🎤 Используем ElevenLabs для генерации голоса")
                
                audio = self.elevenlabs_client.text_to_speech.convert(
                    text=personal_text,
                    voice_id=ELEVENLABS_VOICE_ID,
                    model_id="eleven_multilingual_v2",
                    output_format="mp3_44100_128",
                    voice_settings={
                        "stability": 0.5,
                        "similarity_boost": 0.75,
                        "style": 0.5,
                        "use_speaker_boost": True
                    }
                )
                
                # Сохраняем во временный файл
                temp_audio = tempfile.NamedTemporaryFile(delete=False, suffix='.mp3')
                audio_bytes = b''.join(audio)
                temp_audio.write(audio_bytes)
                temp_audio.close()
                
                logger.info(f"✅ Голосовое сообщение создано: {temp_audio.name}")
                return temp_audio.name
            else:
                # Используем gTTS
                logger.info("🎤 Используем gTTS для генерации голоса")
                return self._generate_voice_with_gtts(personal_text)
            
        except Exception as e:
            logger.error(f"❌ Ошибка при создании голосового сообщения: {e}")
            try:
                personal_text = self.convert_to_personal_voice_style(text)
                return self._generate_voice_with_gtts(personal_text)
            except Exception as e2:
                logger.error(f"❌ Критическая ошибка: {e2}")
                return None
    
    def _generate_voice_with_gtts(self, text):
        """Генерация голоса с помощью gTTS"""
        try:
            from gtts import gTTS
            
            tts = gTTS(text=text, lang='ru', slow=False)
            temp_audio = tempfile.NamedTemporaryFile(delete=False, suffix='.mp3')
            tts.save(temp_audio.name)
            temp_audio.close()
            
            logger.info(f"✅ Голосовое сообщение создано с gTTS: {temp_audio.name}")
            return temp_audio.name
            
        except Exception as e:
            logger.error(f"❌ Ошибка в gTTS: {e}")
            return None
    
    async def send_message_to_channel(self):
        """Отправляет текстовое сообщение в канал"""
        message = self.generate_message()
        max_retries = 3
        
        for attempt in range(max_retries):
            try:
                sent_message = await asyncio.wait_for(
                    self.bot.send_message(
                        chat_id=CHANNEL_ID,
                        text=message
                    ),
                    timeout=30
                )
                self.last_message_id = sent_message.message_id
                logger.info(f"📝 Сообщение отправлено: {message}")
                return
                
            except asyncio.TimeoutError:
                logger.error(f"⏰ Таймаут отправки сообщения (попытка {attempt + 1})")
                if attempt < max_retries - 1:
                    await asyncio.sleep(2 ** attempt)
                continue
            except Exception as e:
                logger.error(f"❌ Ошибка отправки сообщения (попытка {attempt + 1}): {e}")
                if attempt < max_retries - 1:
                    await asyncio.sleep(2 ** attempt)
                else:
                    logger.error(f"💀 Не удалось отправить сообщение")
    
    async def send_voice_message_to_channel(self):
        """Отправляет голосовое сообщение в канал"""
        message = self.generate_message()
        max_retries = 3
        
        # Генерируем голосовое сообщение
        voice_file = self.generate_voice_message(message)
        if not voice_file:
            logger.error("Не удалось создать голосовое сообщение, отправляем текст")
            await self.send_message_to_channel()
            return
        
        for attempt in range(max_retries):
            try:
                with open(voice_file, 'rb') as voice:
                    sent_message = await asyncio.wait_for(
                        self.bot.send_voice(
                            chat_id=CHANNEL_ID,
                            voice=voice,
                            caption=message
                        ),
                        timeout=60
                    )
                self.last_message_id = sent_message.message_id
                logger.info(f"🎤 Голосовое сообщение отправлено: {message}")
                
                # Удаляем временный файл
                try:
                    os.unlink(voice_file)
                except:
                    pass
                return
                
            except asyncio.TimeoutError:
                logger.error(f"⏰ Таймаут отправки голосового сообщения (попытка {attempt + 1})")
                if attempt < max_retries - 1:
                    await asyncio.sleep(2 ** attempt)
                continue
            except Exception as e:
                logger.error(f"❌ Ошибка отправки голосового сообщения (попытка {attempt + 1}): {e}")
                if attempt < max_retries - 1:
                    await asyncio.sleep(2 ** attempt)
                else:
                    logger.error(f"💀 Не удалось отправить голосовое сообщение")
                    try:
                        os.unlink(voice_file)
                    except:
                        pass
                    await self.send_message_to_channel()
                    return
    
    async def send_completion_message_to_channel(self):
        """Отправляет сообщение о завершении работы"""
        message = self.generate_completion_message()
        max_retries = 3
        
        for attempt in range(max_retries):
            try:
                # Если есть ID предыдущего сообщения, отвечаем на него
                if self.last_message_id:
                    sent_message = await asyncio.wait_for(
                        self.bot.send_message(
                            chat_id=CHANNEL_ID,
                            text=message,
                            reply_to_message_id=self.last_message_id
                        ),
                        timeout=30
                    )
                else:
                    sent_message = await asyncio.wait_for(
                        self.bot.send_message(
                            chat_id=CHANNEL_ID,
                            text=message
                        ),
                        timeout=30
                    )
                
                logger.info(f"✅ Ответное сообщение отправлено: {message}")
                return
                
            except asyncio.TimeoutError:
                logger.error(f"⏰ Таймаут отправки ответного сообщения (попытка {attempt + 1})")
                if attempt < max_retries - 1:
                    await asyncio.sleep(2 ** attempt)
                continue
            except Exception as e:
                logger.error(f"❌ Ошибка отправки ответного сообщения (попытка {attempt + 1}): {e}")
                if attempt < max_retries - 1:
                    await asyncio.sleep(2 ** attempt)
                else:
                    logger.error(f"💀 Не удалось отправить ответное сообщение")
    
    def send_message_sync(self):
        """Синхронная обертка для отправки текстового сообщения"""
        try:
            loop = asyncio.new_event_loop()
            asyncio.set_event_loop(loop)
            try:
                loop.run_until_complete(self.send_message_to_channel())
            finally:
                try:
                    loop.close()
                except:
                    pass
        except Exception as e:
            logger.error(f"❌ Ошибка в send_message_sync: {e}")
    
    def send_voice_message_sync(self):
        """Синхронная обертка для отправки голосового сообщения"""
        try:
            loop = asyncio.new_event_loop()
            asyncio.set_event_loop(loop)
            try:
                loop.run_until_complete(self.send_voice_message_to_channel())
            finally:
                try:
                    loop.close()
                except:
                    pass
        except Exception as e:
            logger.error(f"❌ Ошибка в send_voice_message_sync: {e}")
    
    def send_completion_message_sync(self):
        """Синхронная обертка для отправки ответного сообщения"""
        try:
            loop = asyncio.new_event_loop()
            asyncio.set_event_loop(loop)
            try:
                loop.run_until_complete(self.send_completion_message_to_channel())
            finally:
                try:
                    loop.close()
                except:
                    pass
        except Exception as e:
            logger.error(f"❌ Ошибка в send_completion_message_sync: {e}")
    
    def schedule_messages(self):
        """Настраивает расписание отправки сообщений"""
        # 08:00 утра - ГОЛОСОВОЕ СООБЩЕНИЕ #1
        schedule.every().day.at("08:00").do(self.send_voice_message_sync)
        # 11:00 дня - ГОЛОСОВОЕ СООБЩЕНИЕ #2
        schedule.every().day.at("11:00").do(self.send_voice_message_sync)
        # 14:00 дня - ГОЛОСОВОЕ СООБЩЕНИЕ #3
        schedule.every().day.at("14:00").do(self.send_voice_message_sync)
        # 17:00 вечера - ТЕКСТОВОЕ СООБЩЕНИЕ
        schedule.every().day.at("17:00").do(self.send_message_sync)
        # 20:00 вечера - ГОЛОСОВОЕ СООБЩЕНИЕ #4
        schedule.every().day.at("20:00").do(self.send_voice_message_sync)
        # 23:00 вечера - ОТВЕТНОЕ СООБЩЕНИЕ (работа выполнена)
        schedule.every().day.at("23:00").do(self.send_completion_message_sync)
        
        logger.info("📅 Расписание: 08:00 🎤, 11:00 🎤, 14:00 🎤, 17:00 📝, 20:00 🎤, 23:00 ✅")
    
    def run_scheduler(self):
        """Запускает планировщик задач"""
        while True:
            try:
                schedule.run_pending()
                time.sleep(60)
            except Exception as e:
                logger.error(f"❌ Ошибка в планировщике: {e}")
                time.sleep(60)
    
    async def start_bot(self):
        """Запускает бота"""
        try:
            logger.info("🤖 Бот запускается...")
            logger.info(f"📢 Канал: {CHANNEL_ID}")
            
            # Тестовая отправка сообщения
            logger.info("📤 Отправка тестового сообщения...")
            await self.send_message_to_channel()
            
            await asyncio.sleep(5)
            
            # Тестовая отправка голосового сообщения
            logger.info("🎤 Отправка тестового голосового сообщения...")
            await self.send_voice_message_to_channel()
            
            # Настраиваем расписание
            self.schedule_messages()
            
            # Запускаем планировщик в отдельном потоке
            scheduler_thread = Thread(target=self.run_scheduler, daemon=True)
            scheduler_thread.start()
            
            logger.info("✅ Бот успешно запущен!")
            
            # Держим бота активным
            while True:
                await asyncio.sleep(3600)
                
        except Exception as e:
            logger.error(f"❌ Ошибка при запуске бота: {e}")
            raise

def signal_handler(signum, frame):
    """Обработчик сигналов для graceful shutdown"""
    logger.info(f"Получен сигнал {signum}, завершаем работу...")
    sys.exit(0)

def main():
    """Главная функция"""
    signal.signal(signal.SIGINT, signal_handler)
    signal.signal(signal.SIGTERM, signal_handler)

    bot = WorkBot()

    try:
        if os.getenv("RAILWAY_ENVIRONMENT"):
            logger.info("🚀 Запуск в Railway environment")
        
        asyncio.run(bot.start_bot())
    except KeyboardInterrupt:
        logger.info("Бот остановлен пользователем")
    except Exception as e:
        logger.error(f"Критическая ошибка: {e}")
        if os.getenv("RAILWAY_ENVIRONMENT"):
            logger.info("Перезапуск через 30 секунд...")
            time.sleep(30)
            main()
        else:
            raise

if __name__ == "__main__":
    main()
